# API base for menu data (optional; default: https://zaidi123.pythonanywhere.com)
# VITE_API_BASE=https://your-api.com

# Public base URL for AR model files (GLB). Set this if your API returns relative paths
# (e.g. dishes/ar/Egg_3DModel.glb) so Scene Viewer can load the model. Use your S3 or CDN URL.
# VITE_AR_ASSET_BASE=https://your-bucket.s3.me-central-1.amazonaws.com
